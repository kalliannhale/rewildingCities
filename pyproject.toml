[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "rewilding-canopy"
version = "0.1.0"
requires-python = ">=3.13"

[tool.setuptools.packages.find]
include = ["canopy*", "soil*", "roots*", "garden*", "seeds*"]