# rewildingCities Python dependencies
# Install: pip install -r requirements.txt

# Core
pyyaml>=6.0
jsonschema>=4.18.0
referencing>=0.30.0
numpy>=1.24.0
requests>=2.28.0

# Cloud storage (for S3 provider / ESA WorldCover)
boto3>=1.28.0

# Optional: raster operations in Python (for STAC/S3 tile mosaic)
# Uncomment if you need multi-tile mosaic support:
# rasterio>=1.3.0

# Optional: Google Earth Engine (for NTL, population acquisition)
# Requires free GEE account + `earthengine authenticate`
# Uncomment if you need GEE data:
# earthengine-api>=0.1.370